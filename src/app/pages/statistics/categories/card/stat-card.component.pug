.stat-card([class.selected]="current", (click)="onCardClick()")
    .icon-label-box
        .icon-area
            i([class]="'pi ' + icon + ' stat-icon'")
        .label-area
            | {{ label }}